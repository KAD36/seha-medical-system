# إدارة الاشتراكات الشهرية

## تجربة المستخدم

1. يفتح المستخدم البوت ويرسل `/start`.
2. إذا لم يكن اشتراكه فعّالًا، يعرض البوت معرّف Telegram الرقمي وزر التواصل مع `@Yousef_sbri`.
3. يرسل المستخدم معرّفه إلى `@Yousef_sbri` ويتفق معه خارج البوت.
4. بعد تأكيد الاشتراك، يفعّله مسؤول الاشتراكات من المحادثة الخاصة مع البوت.
5. يرسل المستخدم `/start` مرة أخرى ويبدأ الاستخدام.

لا يحتوي البوت على أسعار أو تحصيل مالي، ولا يخزن بيانات دفع.

## أوامر مسؤول الاشتراكات

هذه الأوامر لا تعمل إلا للحساب الموجود في متغير البيئة `ADMIN_USER_ID`:

- `/subscriptions` — عرض دليل أوامر الإدارة.
- `/grant ID` — تفعيل أو تمديد شهر تقويمي واحد.
- `/grant ID MONTHS` — تفعيل أو تمديد مدة من شهر إلى 12 شهرًا.
- `/renew ID` — تمديد شهر تقويمي واحد.
- `/revoke ID` — إلغاء الاشتراك فورًا.
- `/substatus ID` — عرض حالة مستخدم وتاريخ انتهاء اشتراكه.
- `/subscribers` — عرض الاشتراكات الفعّالة.

إذا كان الاشتراك ما زال فعّالًا، تُضاف المدة إلى تاريخ انتهائه الحالي. وإذا كان منتهيًا، تبدأ المدة من وقت التفعيل الجديد.

## أوامر المستخدم

- `/id` — عرض معرّف Telegram المطلوب للتفعيل.
- `/mystatus` — عرض حالة الاشتراك وتاريخ الانتهاء.
- `/start` — بدء الاستخدام بعد التفعيل.

## الحماية

- التفعيل والإلغاء محصوران في `ADMIN_USER_ID` على الخادم.
- جميع وظائف التقارير تتحقق من الاشتراك في كل استخدام.
- مدة الاشتراك يحسبها الخادم، ولا يقبل تاريخ انتهاء يرسله المستخدم.
- المدد المقبولة من شهر إلى 12 شهرًا فقط.
- تُخزن الاشتراكات وسجلات التفعيل والإلغاء في PostgreSQL.
- تُسجل هوية المدير الذي نفذ كل عملية إدارية.
- يمنع البوت استخدام وظائف الاشتراك والتقارير في المجموعات؛ الاستخدام في المحادثة الخاصة فقط.
- التوكنات وكلمات المرور تبقى في متغيرات Render ولا تُحفظ في Git.
